import React from "react";

// TODO: Give a better message based on the connection status
const ConnectMetamask = () => {
	return <div>Please connect to Metamask</div>;
};

export default ConnectMetamask;
